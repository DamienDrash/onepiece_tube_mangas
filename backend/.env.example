# Configuration file for the OnePiece offline downloader.
# Copy this file to `.env` and adjust the values for your environment.

# Directory where downloaded chapters and EPUBs will be stored.  Can
# be relative or absolute.  The default in app.py is ./data.
STORAGE_DIR=./data

# Base URL of the OnePiece‑Tube website.  In most cases you should
# leave this unchanged.  If the site domain changes or you are
# accessing a mirror, update accordingly.
BASE_URL=https://onepiece.tube

# Backend server configuration
PORT=8001
HOST=0.0.0.0

# SMTP settings for sending notification e‑mails.  To disable e‑mail
# notifications simply leave these variables unset or remove them.
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USERNAME=your_smtp_username
SMTP_PASSWORD=your_smtp_password

# Sender address that appears in the From header.  Include a friendly
# name if desired, e.g. "OnePiece Offline <no-reply@example.com>".
SMTP_SENDER=no-reply@example.com
